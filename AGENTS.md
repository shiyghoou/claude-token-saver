# プロジェクト作業ルール

このプロジェクトで作業するエージェントは、以下の手順を必ず守る。

リポジトリ概要は README.md を正とする。段階4（キャリブレーションと診断: `token-report --calibrate` / `token-calibrate --apply`）と段階5（委譲判断ガイド: `skills/delegation-policy`）は実装済みである。作業ルール本文はプロセス規律であり、機能仕様の詳細は README および `docs/` 配下を参照する。引き継ぎ本文の信頼境界と残差リスクは README「pending の運用上の信頼境界（残差リスク）」および `skills/session-handoff/SKILL.md` を正とする。

## 1. Issueファースト

- 作業対象リポジトリのGitHub Issueを、作業を開始する前に必ず新規作成または既存の対象Issueとして確定する。
- 既存の対象Issueまたはプルリクエストがある場合は、open状態で対象範囲が一致することを確認して再利用する。open状態で対象範囲が一致するものがない場合（候補が存在しない場合を含む）は、ユーザーの承認を得て新規Issueを作成し、作成が完了するまで作業を開始しない。
- ここでいう対象範囲一致とは、Issueまたはプルリクエストの目的、変更対象、完了条件が今回の作業と一致することをいう。記載が不足または曖昧な場合は再利用しない。
- Issueまたはプルリクエストの検索・状態確認に失敗した場合は状態不明としてIssue作成・編集を停止し、接続復旧して対象を確認するまで続行しない。
- 対象範囲が一致する候補が複数ある場合は、ユーザーが正規のIssueまたはプルリクエストを選択するまで作業を開始しない。
- 作業指示が明確でない、または不明点がある場合は、Issueを作成する前にブレインストーミングなどで要件を確定する。
- Issueには目的、対象範囲、完了条件を記載する。
- Issueを作成できない場合は、ファイルの編集やその他の変更作業を開始せず、作成できない理由をユーザーへ報告する。

## 2. Issue番号入りブランチで作業する

- Issueを新規作成した、または既存の対象Issue/プルリクエストを再利用すると確定したら、作業用ブランチを準備する。
- 既存プルリクエストを再利用する場合は、headリポジトリから指定head SHAを取得し、そのSHAからローカル作業ブランチを作成・checkoutする。編集前に `git rev-parse HEAD` と指定head SHAが一致することを確認し、不一致なら停止する。この場合に限り、Issue番号を含まないheadブランチを例外として使用できる。既存Issue（プルリクエストなし）を再利用する場合は、`issue-<Issue番号>-...` の形式のブランチを作成する。
- 既存Issueからブランチを作成する場合は、既定ブランチ名をGitHub APIまたは `origin/HEAD` で確定し、最新の既定ブランチをfetchしたうえで、`git rev-parse --verify origin/<既定ブランチ>^{commit}` で完全SHAへ解決する。ユーザーが明示承認した基点SHAを使う場合を除き、そのSHAから作成し、作成後は `git rev-parse HEAD` と基点SHAが一致することを確認する。fetch・ブランチ作成・checkoutのいずれかが失敗した場合は直ちに停止する。
- 作業対象範囲は、Issueまたはプルリクエストに記載された変更対象パスと、その変更に必要なテスト・設定・文書パスとする。ブランチ作成・checkoutの前に、Gitルート、現在のブランチ、および `git status --porcelain=v1 --untracked-files=all --ignored=matching` による未コミット・未追跡・ignoredの変更を確認し、対象範囲と重複していないか確認する。dirtyな変更がある場合はcleanな別worktreeなどで作業・テストを分離する。作業用worktreeを作成した場合は、その中でも同じstatus・ブランチ確認を再実施する。分離できない場合は編集・テスト・stage・コミットせず停止し、ユーザーへ確認を求める。
- Gitルート、`git status`、`git branch --show-current` の取得に失敗した場合、ブランチ名が空の場合、detached HEADの場合、または通常の作業ブランチ名が `issue-<Issue番号>-...` に一致しない場合は、編集・テスト・stage・コミットせず停止する。`main` では直接作業しない。既存PRの確認済みheadブランチだけは、PR再利用の例外として扱う。
- ファイルの編集、テスト、コミットなどの作業は、Issue番号入りブランチ上で行う。
- `main` へ直接編集・コミットしてはならない。
- 作業開始時に、対象リポジトリ、現在のブランチ、未コミット変更を確認する。
- 既存の未コミット変更は、作業対象と無関係であっても保持し、上書き・破棄しない。

## 3. 役割分担

- メインモデルは、オーケストレーター、PM、およびメイン実装者として振る舞う。
- 完成時の別サブエージェントによるレビューは必須とする。それ以外の独立した調査、重い作業、専門的な確認などの委譲は、作業内容とトークン消費コストを見極めたうえで必要な場合に依頼する。委譲の判断基準が必要なときは、先に `skills/delegation-policy` を参照する。
- サブエージェントへ依頼する場合は、目的、担当範囲、期待する成果物を明確にする。

## 4. 別エージェントによるレビュー

- 変更または作業成果物が完成したら、実装を担当したモデル・エージェントとは別のサブエージェントにレビューを依頼する。
- 作業内容に応じたレビュー（文書なら正確性・整合性、コードなら動作・テスト、設定なら適用範囲・安全性など）を行い、これに加えて要件漏れ、既存機能への影響、境界条件、テスト不足を探す敵対的レビューを行う。
- レビュー結果と対応内容は、オープンな対象プルリクエストがある場合はそのレビューコメントに、ない場合は対象Issueのコメントに記録する。記録にはレビュアー識別子、Base SHA、Head SHA、対象コミット、指摘ごとの優先度、blocking判定、対応内容、再レビュー結果を含め、指摘がない場合も「指摘なし」と明記する。
- 別サブエージェントを起動できない、実装者と別であることを確認できない、またはレビュー結果を回収できない場合は、完了宣言とマージ依頼を停止してユーザーへ報告する。
- Issueまたはプルリクエストへのレビュー記録に失敗した場合も、完了・マージ可能判定を行わず、失敗理由と未記録状態をユーザーへ報告する。

## 5. 検証・修正・再レビュー

- PRがある場合はGitHub APIまたはCLIでPRのbase.shaとhead.shaを取得し、実際のコミットとして解決・固定する。PRがない場合は、既定ブランチ名をGitHub APIまたは `origin/HEAD` で確定し、最新の既定ブランチをfetchしたうえで、`git rev-parse --verify origin/<既定ブランチ>^{commit}` から完全SHAを解決する。そのSHAと対象ブランチのHEADのmerge-baseをBase、対象ブランチのHEADをHeadとし、実際のSHAを解決する。いずれの場合も、BaseとHeadの実際のSHAをレビュー記録に残す。
- GitHub API/CLI、fetch、ブランチ作成・checkout、`git rev-parse`、`git merge-base`、または差分検証のいずれかが失敗した場合は、レビュー記録・完了宣言・マージ依頼を停止してユーザーへ報告する。
- 検証は `git rev-parse --show-toplevel` で確認したGitルートで実行する。コミット前は `git diff --cached --no-renames --name-only`、レビュー対象は実際に解決・記録したSHAを使う `git diff --no-renames --name-only <BASE>..<HEAD>` で変更パスを確認し、Issue/PRの対象範囲外のパスがあれば停止またはユーザーの承認を得る。
- 実装後は、変更内容に応じたテスト、静的検査、差分確認などを実行する。シェル・Python・フック・スキル・installer を変更した場合は、関連する個別テストに加え、可能な範囲で `./test/run.sh` を実行する。いずれか一方でも `AGENTS.md` または `CLAUDE.md` を変更した場合は、作業ツリーでは常に `cmp -s AGENTS.md CLAUDE.md`、stage済み内容では `git rev-parse --verify :AGENTS.md` と `git rev-parse --verify :CLAUDE.md` の成功、それぞれについて `test -n "$(git diff --cached --no-renames --name-only -- AGENTS.md)"` と `test -n "$(git diff --cached --no-renames --name-only -- CLAUDE.md)"` の成功、および `test "$(git rev-parse :AGENTS.md)" = "$(git rev-parse :CLAUDE.md)"` で内容の一致を確認する。コミット直前には `git diff --exit-code -- AGENTS.md CLAUDE.md` で作業ツリーとindexの一致も確認する。また、コミット前の作業差分には `git diff --no-renames --check`、stage済み差分には `git diff --cached --no-renames --check`、レビュー対象のBase..Head差分には、実際に解決・記録したSHAを `<BASE>` と `<HEAD>` に代入した `git diff --no-renames --check <BASE>..<HEAD>` を実行する。
- ここでいうblockingな問題とは、要件違反、対象変更の機能不全・安全性問題、対象変更に関連する検証失敗、または要件・影響・再現条件などの根拠を伴ってレビューで必須修正と判断された指摘をいう。単なるスタイル上の提案はblockingとしない。該当する問題が見つかった場合は、修正してから再度検証とレビューを行う。
- 対象変更と無関係な既存の問題は、対象Issueまたはプルリクエストのコメントに内容を記録したうえで、今回の作業では変更しない。
- 対象変更についてマージ可能な品質になるまで、blockingな問題がなくなることを確認するループを繰り返す。
- 完了を宣言する前に、実行した検証コマンドと結果を確認する。

## 6. 直接マージの禁止

- エージェントは、プルリクエストを直接マージしてはならない。
- マージ可能な状態になったら、変更内容、検証結果、レビュー結果をユーザーへ報告し、ユーザーにマージを依頼する。

## 7. AGENTS.md と CLAUDE.md の整合

- `AGENTS.md` と `CLAUDE.md` は、このプロジェクトの作業ルールについてバイト単位で同じ内容を保つ。
- 一方だけを更新せず、変更時は両ファイルを同時に更新し、`cmp -s AGENTS.md CLAUDE.md` で確認する。
